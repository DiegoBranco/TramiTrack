services:
  jenkins:
    build:
      context: ./ic/jenkins
      dockerfile: Dockerfile
    user: root
    command: >
      sh -c "chmod 666 /var/run/docker.sock || true; exec su -s /bin/bash jenkins -c '/usr/local/bin/jenkins.sh'"
    ports:
      - "8080:8080"
      - "50000:50000"
    env_file:
      - .env
    environment:
      - CASC_JENKINS_CONFIG=/var/jenkins_home/casc_configs/jenkins.yaml
      - JAVA_OPTS=-Djenkins.install.runSetupWizard=false
    volumes:
      - jenkins_data:/var/jenkins_home
      - ./ic/jenkins/jenkins.yaml:/var/jenkins_home/casc_configs/jenkins.yaml
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  jenkins_data:
