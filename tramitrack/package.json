{
    "scripts": {
    "dev": "docker-compose up",
    "dev:jenkins": "docker-compose -f docker-compose.yml -f docker-compose.jenkins.yml up",
    "down": "docker-compose down",
    "logs": "docker-compose logs -f"
    }
}